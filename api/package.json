{
  "name": "powershell-learning-api",
  "version": "1.0.0",
  "description": "Azure Functions API for PowerShell learning sandbox",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^5.0.0"
  }
}
